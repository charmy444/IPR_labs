apiVersion: v1
kind: Secret
metadata:
  name: sda-secret
  namespace: {{ .Release.Namespace }}
  labels:
    {{- range $key, $value := .Values.labels }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
type: Opaque
stringData:
  SDA_DEMO_SECRET_KEY: {{ .Values.secret.demoSecretKey | quote }}

