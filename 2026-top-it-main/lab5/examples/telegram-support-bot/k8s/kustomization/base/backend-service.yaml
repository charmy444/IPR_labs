apiVersion: v1
kind: Service
metadata:
  name: telegram-backend
  labels:
    app: telegram-support-bot
    component: backend
spec:
  selector:
    app: telegram-support-bot
    component: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
