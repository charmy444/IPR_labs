apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: telegram-prod

resources:
  - ../../base
  - secret.yaml

patches:
  - path: patch-resources.yaml

commonLabels:
  environment: prod
