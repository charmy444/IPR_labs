apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: telegram-demo

resources:
  - ../../base
  - secret.yaml

patches:
  - path: patch-replicas.yaml

commonLabels:
  environment: dev
